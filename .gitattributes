# 统一行尾：仓库里一律 LF。
# CI 同时跑 ubuntu 和 windows，不能让行尾随平台漂（自检里有大量按行匹配的断言）。
* text=auto eol=lf

# 二进制：别做任何行尾/编码转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
*.tgz  binary
